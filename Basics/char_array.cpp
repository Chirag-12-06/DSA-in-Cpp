#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main(){
    string s;
    getline(cin,s);
    cout<<s<<s.size(); 

    //REVERSE A STRING
    // string s,r;
    // getline(cin,s);
    // reverse(s.begin(),s.end());
    // cout<<s;

    //SUBSTRING OF A STRING
    // string s,r;
    // getline(cin,s);
    // r=s.substr(0,3);
    // cout<<r<<" "<<s.substr(1);


}